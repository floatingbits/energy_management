[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "event-contracts"
version = "0.1.0"
description = "Shared event contracts for the Energy Trading Platform"
requires-python = ">=3.12"

dependencies = [
    "pydantic>=2,<3",
]